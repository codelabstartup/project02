export default function BoardWrite() {
  // 1) useState 4개 정도: title, writer, password, content
  // 2) handleSubmit 함수 만들기 (e.preventDefault() 필수)
  // 3) axios.post(...) 로 서버에 전송
  // 4) 성공하면 alert 후 navigate("/board")

  return <div>{/* 제목/작성자/비밀번호/내용 입력 + 등록 버튼 */}</div>
}
